1. What is the difference between `@Component`, `@Service`, `@Repository`, and `@Controller`?
2. How does dependency injection work in Spring?
3. What is the purpose of `ApplicationContext` and how is it different from `BeanFactory`?
4. What are the different bean scopes in Spring?
5. What does the `@Autowired` annotation do?
6. How does `@Qualifier` resolve ambiguity in dependency injection?
7. What is the role of `@Primary` in Spring?
8. What is the bean lifecycle and how can you hook into it?
9. How does `@PostConstruct` and `@PreDestroy` work?
10. What is the purpose of `@Lazy` and `@DependsOn`?
11. What does the `@SpringBootApplication` annotation include?
12. How does Spring Boot's auto-configuration mechanism work?
13. What is the role of `@EnableAutoConfiguration`?
14. What is the difference between `@Bean` and `@Component`?
15. How does `@ConfigurationProperties` work in Spring Boot?
16. What are Spring Profiles and how do you use them?
17. What are conditional annotations like `@ConditionalOnProperty` used for?
18. What is the difference between `.properties` and `.yaml` configuration files?
19. How do you inject values from configuration files into beans?
20. What is a custom Spring Boot starter and how do you build one?
21. What is the difference between `@Controller` and `@RestController`?
22. What is the purpose of `@RequestParam`, `@PathVariable`, and `@RequestBody`?
23. What is content negotiation in Spring MVC?
24. How do you implement global exception handling using `@ControllerAdvice`?
25. What is the use of `@Valid` and how does validation work in Spring?
26. What is a query method in Spring Data JPA?
27. What are the differences between `CrudRepository`, `JpaRepository`, and `PagingAndSortingRepository`?
28. How do you write custom queries using `@Query` in Spring Data?
29. What is lazy loading and eager loading in JPA?
30. What is the N+1 select problem and how do you avoid it?
31. How does transaction management work in Spring?
32. What is the purpose of the `@Transactional` annotation?
33. What are propagation types in Spring transactions?
34. What are isolation levels in Spring transactions?
35. How does rollback behavior work with `@Transactional`?
36. How do you define a read-only transaction in Spring?
37. What is Spring AOP and what problems does it solve?
38. How do you define an aspect using `@Aspect` and `@Around`?
39. How do you implement logging with Spring AOP?
40. What are the differences between RestTemplate and WebClient?
41. How do you make HTTP calls using WebClient?
42. How does Spring Boot handle external configuration?
43. How do you use `@Value` to inject a property?
44. What is the purpose of `CommandLineRunner` and `ApplicationRunner`?
45. How does exception handling work in Spring REST APIs?
46. How do you secure a Spring Boot application using Spring Security?
47. How do you restrict access to controller methods using roles?
48. What is the difference between `@Secured` and `@PreAuthorize`?
49. What is Spring Boot Actuator and what can it monitor?
50. How do you implement a custom filter or interceptor in Spring Boot?